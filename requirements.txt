# Base dependencies — installable everywhere (incl. the Ubuntu CI runner).
# The MLX-native generation stack (mflux, mlx-lm, mlx-vlm) is Apple-Silicon-only
# and lives in requirements-mac.txt. Install both on a Mac:
#   pip install -r requirements.txt -r requirements-mac.txt
flask>=3.0
Pillow>=10.0
cairosvg>=2.7
numpy>=1.26  # frame compositing (Showcase/LOTR masks) + color_transfer
pytest>=8.0
pytest-timeout>=2.0
